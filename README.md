# Data directory

This public directory intentionally contains no empirical data.

Real administrative data, direct identifiers, and linkage keys must remain
outside GitHub in access-controlled storage. Only synthetic examples belong
in `examples/`.
